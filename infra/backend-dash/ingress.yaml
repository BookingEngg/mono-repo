apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-dash-ingress
  annotations:
    kubernetes.io/ingress.class: "gce"
spec:
  ingressClassName: gce
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: backend-dash-service
                port:
                  number: 80
