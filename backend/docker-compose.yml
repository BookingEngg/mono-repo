version: "3.8"

services:
  backend:
    build:
      context: ./
      args:
        GITHUB_PAT: "${GITHUB_PAT}"
    container_name: backend
    restart: always
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
